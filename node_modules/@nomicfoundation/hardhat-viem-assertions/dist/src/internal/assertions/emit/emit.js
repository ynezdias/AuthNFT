import { handleEmit } from "./core.js";
export async function emit(viem, contractFn, contract, eventName) {
    await handleEmit(viem, contractFn, contract, eventName);
}
//# sourceMappingURL=emit.js.map