{
  "name": "certifier",
  "version": "1.0.0",
  "description": "Certificate NFT (ERC-721) with role-based issuers",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "node": "npx hardhat node",
    "deploy:sepolia": "npx hardhat run --network sepolia scripts/deploy.js",
    "deploy:localhost": "npx hardhat run --network localhost scripts/deploy.js"
  },
  "author": "you",
  "license": "MIT",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^3.1.0",
    "chai": "^4.3.7",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0",
    "hardhat": "^2.17.0",
    "@openzeppelin/contracts": "^4.9.3"
  }
}
